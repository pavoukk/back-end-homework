/**
 * Package contains State, Pair and Command definition, transitions of one state to other, Command manager that sets
 * a command for every state.
 */
package it.sevenbits.formatter.lexer.statemachine;